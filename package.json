{
  "name": "github-issues-kanban",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm run dev:server & pnpm run dev:client",
    "dev:server": "pnpm --filter @workspace/server run dev",
    "dev:client": "pnpm --filter @workspace/client run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./packages/**\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
